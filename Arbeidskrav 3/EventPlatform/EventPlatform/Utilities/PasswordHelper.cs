namespace EventPlatform.Utilities;

/// <summary>
/// Handles password hashing and verification using BCrypt.
/// </summary>
public static class PasswordHelper
{
    /// <summary>Hashes a plain-text password.</summary>
    public static string Hash(string password) => throw new NotImplementedException();

    /// <summary>Verifies a plain-text password against a stored hash.</summary>
    public static bool Verify(string password, string hash) => throw new NotImplementedException();
}
